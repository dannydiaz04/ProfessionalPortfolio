{
    "version": 2,
    "builds": [{ "src": "package.json", "use": "@vercel/static-build" }],
    "routes": [{ "src": "/(.*)", "dest": "/" }],
    "build": { "command": "npm run build", "outputDirectory": "dist" }
  }
  